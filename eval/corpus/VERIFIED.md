# Verified reverse-of-fix candidates

This directory is offline-only and hand-authored unless explicitly marked.

## Status legend
- `⚠️ placeholder` — hand-authored/curated case pending real mined provenance.
- `✅ verified-from-source` — verified against a real mined source artifact (if applicable).

## Current status

- All 20 corpus entries are placeholders and this branch does not assert private or post-cutoff provenance.
- `clean` entries are intended as behavior-preserving refactors.
- `buggy` entries intentionally preserve a single labeled defect, including one noisy off-by-one anchor case (`js-009`).
